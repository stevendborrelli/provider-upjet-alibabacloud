---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: chains.cr.alibabacloud.crossplane.io
spec:
  group: cr.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: Chain
    listKind: ChainList
    plural: chains
    singular: chain
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Chain is the Schema for the Chains API. Provides a Alicloud CR
          Chain resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChainSpec defines the desired state of Chain
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  chainConfig:
                    description: 'The configuration of delivery chain. See chain_config
                      below. NOTE: This parameter must specify the correct value,
                      otherwise the created resource will be incorrect.'
                    items:
                      properties:
                        nodes:
                          description: Each node in the delivery chain. See nodes
                            below.
                          items:
                            properties:
                              enable:
                                description: 'Whether to enable the delivery chain
                                  node. Valid values: true, false.'
                                type: boolean
                              nodeConfig:
                                description: The configuration of delivery chain node.
                                  See node_config below.
                                items:
                                  properties:
                                    denyPolicy:
                                      description: 'Blocking rules for scanning nodes
                                        in delivery chain nodes. See deny_policy below.
                                        Note: When node_name is VULNERABILITY_SCANNING,
                                        the parameters in deny_policy need to be filled
                                        in.'
                                      items:
                                        properties:
                                          action:
                                            description: 'The action of trigger blocking.
                                              Valid values: BLOCK, BLOCK_RETAG, BLOCK_DELETE_TAG.
                                              While Block means block the delivery
                                              chain from continuing to execute, BLOCK_RETAG
                                              means block overwriting push image tag,
                                              BLOCK_DELETE_TAG means block deletion
                                              of mirror tags.'
                                            type: string
                                          issueCount:
                                            description: The count of scanning vulnerabilities
                                              that triggers blocking.
                                            type: string
                                          issueLevel:
                                            description: 'The level of scanning vulnerability
                                              that triggers blocking. Valid values:
                                              LOW, MEDIUM, HIGH, UNKNOWN.'
                                            type: string
                                          logic:
                                            description: 'The logic of trigger blocking.
                                              Valid values: AND, OR.'
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              nodeName:
                                description: 'The name of node. Valid values: DOCKER_IMAGE_BUILD,
                                  DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING, ACTIVATE_REPLICATION,
                                  TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                type: string
                            type: object
                          type: array
                        routers:
                          description: Execution sequence relationship between delivery
                            chain nodes. See routers below.
                          items:
                            properties:
                              from:
                                description: Source node. See from below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                              to:
                                description: Destination node. See to below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  chainName:
                    description: The name of delivery chain. The length of the name
                      is 1-64 characters, lowercase English letters and numbers, and
                      the separators "_", "-", "." can be used, noted that the separator
                      cannot be at the first or last position.
                    type: string
                  description:
                    description: The description delivery chain.
                    type: string
                  instanceId:
                    description: The ID of CR Enterprise Edition instance.
                    type: string
                  instanceIdRef:
                    description: Reference to a EeInstance in cr to populate instanceId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  instanceIdSelector:
                    description: Selector for a EeInstance in cr to populate instanceId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  repoName:
                    description: 'The name of CR Enterprise Edition repository. NOTE:
                      This parameter must specify a correct value, otherwise the created
                      resource will be incorrect.'
                    type: string
                  repoNameRef:
                    description: Reference to a EeRepo in cr to populate repoName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  repoNameSelector:
                    description: Selector for a EeRepo in cr to populate repoName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  repoNamespaceName:
                    description: 'The name of CR Enterprise Edition namespace. NOTE:
                      This parameter must specify the correct value, otherwise the
                      created resource will be incorrect.'
                    type: string
                  repoNamespaceNameRef:
                    description: Reference to a EeNamespace in cr to populate repoNamespaceName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  repoNamespaceNameSelector:
                    description: Selector for a EeNamespace in cr to populate repoNamespaceName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  chainConfig:
                    description: 'The configuration of delivery chain. See chain_config
                      below. NOTE: This parameter must specify the correct value,
                      otherwise the created resource will be incorrect.'
                    items:
                      properties:
                        nodes:
                          description: Each node in the delivery chain. See nodes
                            below.
                          items:
                            properties:
                              enable:
                                description: 'Whether to enable the delivery chain
                                  node. Valid values: true, false.'
                                type: boolean
                              nodeConfig:
                                description: The configuration of delivery chain node.
                                  See node_config below.
                                items:
                                  properties:
                                    denyPolicy:
                                      description: 'Blocking rules for scanning nodes
                                        in delivery chain nodes. See deny_policy below.
                                        Note: When node_name is VULNERABILITY_SCANNING,
                                        the parameters in deny_policy need to be filled
                                        in.'
                                      items:
                                        properties:
                                          action:
                                            description: 'The action of trigger blocking.
                                              Valid values: BLOCK, BLOCK_RETAG, BLOCK_DELETE_TAG.
                                              While Block means block the delivery
                                              chain from continuing to execute, BLOCK_RETAG
                                              means block overwriting push image tag,
                                              BLOCK_DELETE_TAG means block deletion
                                              of mirror tags.'
                                            type: string
                                          issueCount:
                                            description: The count of scanning vulnerabilities
                                              that triggers blocking.
                                            type: string
                                          issueLevel:
                                            description: 'The level of scanning vulnerability
                                              that triggers blocking. Valid values:
                                              LOW, MEDIUM, HIGH, UNKNOWN.'
                                            type: string
                                          logic:
                                            description: 'The logic of trigger blocking.
                                              Valid values: AND, OR.'
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              nodeName:
                                description: 'The name of node. Valid values: DOCKER_IMAGE_BUILD,
                                  DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING, ACTIVATE_REPLICATION,
                                  TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                type: string
                            type: object
                          type: array
                        routers:
                          description: Execution sequence relationship between delivery
                            chain nodes. See routers below.
                          items:
                            properties:
                              from:
                                description: Source node. See from below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                              to:
                                description: Destination node. See to below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  chainName:
                    description: The name of delivery chain. The length of the name
                      is 1-64 characters, lowercase English letters and numbers, and
                      the separators "_", "-", "." can be used, noted that the separator
                      cannot be at the first or last position.
                    type: string
                  description:
                    description: The description delivery chain.
                    type: string
                  instanceId:
                    description: The ID of CR Enterprise Edition instance.
                    type: string
                  instanceIdRef:
                    description: Reference to a EeInstance in cr to populate instanceId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  instanceIdSelector:
                    description: Selector for a EeInstance in cr to populate instanceId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  repoName:
                    description: 'The name of CR Enterprise Edition repository. NOTE:
                      This parameter must specify a correct value, otherwise the created
                      resource will be incorrect.'
                    type: string
                  repoNameRef:
                    description: Reference to a EeRepo in cr to populate repoName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  repoNameSelector:
                    description: Selector for a EeRepo in cr to populate repoName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  repoNamespaceName:
                    description: 'The name of CR Enterprise Edition namespace. NOTE:
                      This parameter must specify the correct value, otherwise the
                      created resource will be incorrect.'
                    type: string
                  repoNamespaceNameRef:
                    description: Reference to a EeNamespace in cr to populate repoNamespaceName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  repoNamespaceNameSelector:
                    description: Selector for a EeNamespace in cr to populate repoNamespaceName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.chainName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.chainName)
                || (has(self.initProvider) && has(self.initProvider.chainName))'
          status:
            description: ChainStatus defines the observed state of Chain.
            properties:
              atProvider:
                properties:
                  chainConfig:
                    description: 'The configuration of delivery chain. See chain_config
                      below. NOTE: This parameter must specify the correct value,
                      otherwise the created resource will be incorrect.'
                    items:
                      properties:
                        nodes:
                          description: Each node in the delivery chain. See nodes
                            below.
                          items:
                            properties:
                              enable:
                                description: 'Whether to enable the delivery chain
                                  node. Valid values: true, false.'
                                type: boolean
                              nodeConfig:
                                description: The configuration of delivery chain node.
                                  See node_config below.
                                items:
                                  properties:
                                    denyPolicy:
                                      description: 'Blocking rules for scanning nodes
                                        in delivery chain nodes. See deny_policy below.
                                        Note: When node_name is VULNERABILITY_SCANNING,
                                        the parameters in deny_policy need to be filled
                                        in.'
                                      items:
                                        properties:
                                          action:
                                            description: 'The action of trigger blocking.
                                              Valid values: BLOCK, BLOCK_RETAG, BLOCK_DELETE_TAG.
                                              While Block means block the delivery
                                              chain from continuing to execute, BLOCK_RETAG
                                              means block overwriting push image tag,
                                              BLOCK_DELETE_TAG means block deletion
                                              of mirror tags.'
                                            type: string
                                          issueCount:
                                            description: The count of scanning vulnerabilities
                                              that triggers blocking.
                                            type: string
                                          issueLevel:
                                            description: 'The level of scanning vulnerability
                                              that triggers blocking. Valid values:
                                              LOW, MEDIUM, HIGH, UNKNOWN.'
                                            type: string
                                          logic:
                                            description: 'The logic of trigger blocking.
                                              Valid values: AND, OR.'
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                              nodeName:
                                description: 'The name of node. Valid values: DOCKER_IMAGE_BUILD,
                                  DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING, ACTIVATE_REPLICATION,
                                  TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                type: string
                            type: object
                          type: array
                        routers:
                          description: Execution sequence relationship between delivery
                            chain nodes. See routers below.
                          items:
                            properties:
                              from:
                                description: Source node. See from below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                              to:
                                description: Destination node. See to below.
                                items:
                                  properties:
                                    nodeName:
                                      description: 'The name of node. Valid values:
                                        DOCKER_IMAGE_BUILD, DOCKER_IMAGE_PUSH, VULNERABILITY_SCANNING,
                                        ACTIVATE_REPLICATION, TRIGGER, SNAPSHOT, TRIGGER_SNAPSHOT.'
                                      type: string
                                  type: object
                                type: array
                            type: object
                          type: array
                      type: object
                    type: array
                  chainId:
                    description: Delivery chain ID.
                    type: string
                  chainName:
                    description: The name of delivery chain. The length of the name
                      is 1-64 characters, lowercase English letters and numbers, and
                      the separators "_", "-", "." can be used, noted that the separator
                      cannot be at the first or last position.
                    type: string
                  description:
                    description: The description delivery chain.
                    type: string
                  id:
                    description: The resource ID of Chain. The value formats as <instance_id>:<chain_id>.
                    type: string
                  instanceId:
                    description: The ID of CR Enterprise Edition instance.
                    type: string
                  repoName:
                    description: 'The name of CR Enterprise Edition repository. NOTE:
                      This parameter must specify a correct value, otherwise the created
                      resource will be incorrect.'
                    type: string
                  repoNamespaceName:
                    description: 'The name of CR Enterprise Edition namespace. NOTE:
                      This parameter must specify the correct value, otherwise the
                      created resource will be incorrect.'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
