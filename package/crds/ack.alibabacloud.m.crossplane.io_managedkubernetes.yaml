---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: managedkubernetes.ack.alibabacloud.m.crossplane.io
spec:
  group: ack.alibabacloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: ManagedKubernetes
    listKind: ManagedKubernetesList
    plural: managedkubernetes
    singular: managedkubernetes
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ManagedKubernetes is the Schema for the ManagedKubernetess API.
          Provides a Alicloud resource to manage container managed kubernetes cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ManagedKubernetesSpec defines the desired state of ManagedKubernetes
            properties:
              forProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  auditLogConfig:
                    description: Audit log configuration. See audit_log_config below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable audit logging. Valid values:
                            true, false.'
                          type: boolean
                        slsProjectName:
                          description: The SLS project to which the Logstore storing
                            the cluster audit logs belongs.
                          type: string
                      type: object
                    type: array
                  autoMode:
                    description: Auto mode cluster configuration. See auto_mode below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable auto mode. Valid values:
                            true, false. Only ACK managed Pro clusters support Auto
                            Mode.'
                          type: boolean
                      type: object
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,
                      which can be empty. Valid values:'
                    type: string
                  controlPlaneEndpointsConfig:
                    description: The cluster access configuration. See control_plane_endpoints_config
                      below.
                    items:
                      properties:
                        internalDnsConfig:
                          description: The cluster internal domain name configuration,
                            applicable to ACK managed clusters. See internal_dns_config
                            below.
                          items:
                            properties:
                              bindVpcs:
                                description: The list of VPCs where the API Server
                                  access domain name takes effect. By default, the
                                  VPC of the cluster is included.
                                items:
                                  type: string
                                type: array
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                        loadBalancersConfig:
                          description: The load balancing configuration for cluster
                            access. See load_balancers_config below.
                          items:
                            properties:
                              endpointType:
                                description: 'The endpoint type. Valid values: private,
                                  public.'
                                type: string
                              loadBalancerId:
                                description: The ID of the SLB (NLB) instance associated
                                  with the endpoint.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  disableEncryption:
                    description: Whether to disable encryption for Kubernetes Secrets.
                      Default value is false. Set to true to disable encryption.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                    type: string
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window. Managed node pool
                      will use it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  operationPolicy:
                    description: The cluster automatic operation policy, only works
                      when maintenance_window is enabled. See operation_policy below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  podVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate podVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  podVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      podVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  profile:
                    description: 'The profile of cluster. Valid values:'
                    type: string
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority. For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  upgradePolicy:
                    description: Configuration block for cluster upgrade operations.
                      See upgrade_policy below.
                    items:
                      properties:
                        controlPlaneOnly:
                          description: 'Whether to upgrade only the control plane
                            without upgrading worker nodes. Valid values: true, false.
                            When set to true, only the cluster control plane components
                            will be upgraded, and worker nodes will remain at their
                            current version. Default is false.'
                          type: boolean
                      type: object
                    type: array
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vswitchIds:
                    description: The vSwitches of the control plane.
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  auditLogConfig:
                    description: Audit log configuration. See audit_log_config below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable audit logging. Valid values:
                            true, false.'
                          type: boolean
                        slsProjectName:
                          description: The SLS project to which the Logstore storing
                            the cluster audit logs belongs.
                          type: string
                      type: object
                    type: array
                  autoMode:
                    description: Auto mode cluster configuration. See auto_mode below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable auto mode. Valid values:
                            true, false. Only ACK managed Pro clusters support Auto
                            Mode.'
                          type: boolean
                      type: object
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,
                      which can be empty. Valid values:'
                    type: string
                  controlPlaneEndpointsConfig:
                    description: The cluster access configuration. See control_plane_endpoints_config
                      below.
                    items:
                      properties:
                        internalDnsConfig:
                          description: The cluster internal domain name configuration,
                            applicable to ACK managed clusters. See internal_dns_config
                            below.
                          items:
                            properties:
                              bindVpcs:
                                description: The list of VPCs where the API Server
                                  access domain name takes effect. By default, the
                                  VPC of the cluster is included.
                                items:
                                  type: string
                                type: array
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                        loadBalancersConfig:
                          description: The load balancing configuration for cluster
                            access. See load_balancers_config below.
                          items:
                            properties:
                              endpointType:
                                description: 'The endpoint type. Valid values: private,
                                  public.'
                                type: string
                              loadBalancerId:
                                description: The ID of the SLB (NLB) instance associated
                                  with the endpoint.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  disableEncryption:
                    description: Whether to disable encryption for Kubernetes Secrets.
                      Default value is false. Set to true to disable encryption.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                    type: string
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window. Managed node pool
                      will use it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  operationPolicy:
                    description: The cluster automatic operation policy, only works
                      when maintenance_window is enabled. See operation_policy below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  podVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate podVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  podVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      podVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  profile:
                    description: 'The profile of cluster. Valid values:'
                    type: string
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority. For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  upgradePolicy:
                    description: Configuration block for cluster upgrade operations.
                      See upgrade_policy below.
                    items:
                      properties:
                        controlPlaneOnly:
                          description: 'Whether to upgrade only the control plane
                            without upgrading worker nodes. Valid values: true, false.
                            When set to true, only the cluster control plane components
                            will be upgraded, and worker nodes will remain at their
                            current version. Default is false.'
                          type: boolean
                      type: object
                    type: array
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vswitchIds:
                    description: The vSwitches of the control plane.
                    items:
                      type: string
                    type: array
                  vswitchIdsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ManagedKubernetesStatus defines the observed state of ManagedKubernetes.
            properties:
              atProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: If this parameter is left empty, no configurations
                            are required. For more config information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: It specifies whether to disable automatic installation.
                          type: boolean
                        name:
                          description: This parameter specifies the name of the component.
                          type: string
                        version:
                          description: It specifies the version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature (requires
                      specifying service_account_issuer as well. From cluster version
                      1.22, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  auditLogConfig:
                    description: Audit log configuration. See audit_log_config below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable audit logging. Valid values:
                            true, false.'
                          type: boolean
                        slsProjectName:
                          description: The SLS project to which the Logstore storing
                            the cluster audit logs belongs.
                          type: string
                      type: object
                    type: array
                  autoMode:
                    description: Auto mode cluster configuration. See auto_mode below.
                    items:
                      properties:
                        enabled:
                          description: 'Whether to enable auto mode. Valid values:
                            true, false. Only ACK managed Pro clusters support Auto
                            Mode.'
                          type: boolean
                      type: object
                    type: array
                  certificateAuthority:
                    description: (List, Deprecated from v1.248.0) Nested attribute
                      containing certificate authority data for your cluster. Please
                      use the attribute certificate_authority of new DataSource alicloud_cs_cluster_credential
                      to replace it.
                    items:
                      properties:
                        clientCert:
                          description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                            is recommended to manage cluster's kubeconfig, you can
                            also save the certificate_authority.client_cert attribute
                            content of new DataSource alicloud_cs_cluster_credential
                            to an appropriate path(like ~/.kube/client-cert.pem) for
                            replace it.
                          type: string
                        clientKey:
                          description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                            is recommended to manage cluster's kubeconfig, you can
                            also save the certificate_authority.client_key attribute
                            content of new DataSource alicloud_cs_cluster_credential
                            to an appropriate path(like ~/.kube/client-key.pem) for
                            replace it.
                          type: string
                        clusterCert:
                          description: The base64 encoded cluster certificate data
                            required to communicate with your cluster. Add this to
                            the certificate-authority-data section of the kubeconfig
                            file for your cluster.
                          type: string
                      type: object
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  clusterSpec:
                    description: 'The cluster specifications of kubernetes cluster,
                      which can be empty. Valid values:'
                    type: string
                  connections:
                    description: A list of kubernetes cluster connection information.
                    items:
                      properties:
                        apiServerInternet:
                          description: API Server Internet endpoint.
                          type: string
                        apiServerIntranet:
                          description: API Server Intranet endpoint.
                          type: string
                        masterPublicIp:
                          description: Master node SSH IP address.
                          type: string
                        serviceDomain:
                          description: Service Access Domain.
                          type: string
                      type: object
                    type: array
                  controlPlaneEndpointsConfig:
                    description: The cluster access configuration. See control_plane_endpoints_config
                      below.
                    items:
                      properties:
                        internalDnsConfig:
                          description: The cluster internal domain name configuration,
                            applicable to ACK managed clusters. See internal_dns_config
                            below.
                          items:
                            properties:
                              bindVpcs:
                                description: The list of VPCs where the API Server
                                  access domain name takes effect. By default, the
                                  VPC of the cluster is included.
                                items:
                                  type: string
                                type: array
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                        loadBalancersConfig:
                          description: The load balancing configuration for cluster
                            access. See load_balancers_config below.
                          items:
                            properties:
                              endpoint:
                                description: (Computed) The access address.
                                type: string
                              endpointType:
                                description: 'The endpoint type. Valid values: private,
                                  public.'
                                type: string
                              loadBalancerId:
                                description: The ID of the SLB (NLB) instance associated
                                  with the endpoint.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  controlPlaneLogComponents:
                    description: List of target components for which logs need to
                      be collected. Supports apiserver, kcm, scheduler, ccm and controlplane-events.
                    items:
                      type: string
                    type: array
                  controlPlaneLogProject:
                    description: Control plane log project. If this field is not set,
                      a log service project named k8s-log-{ClusterID} will be automatically
                      created.
                    type: string
                  controlPlaneLogTtl:
                    description: 'Control plane log retention duration (unit: day).
                      Default 30. If control plane logs are to be collected, control_plane_log_ttl
                      and control_plane_log_components must be specified.'
                    type: string
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  disableEncryption:
                    description: Whether to disable encryption for Kubernetes Secrets.
                      Default value is false. Set to true to disable encryption.
                    type: boolean
                  enableRrsa:
                    description: Whether to enable cluster to support RRSA for kubernetes
                      version 1.22.3+. Default to false. Once the RRSA function is
                      turned on, it is not allowed to turn off. If your cluster has
                      enabled this function, please manually modify your tf file and
                      add the rrsa configuration to the file, learn more RAM Roles
                      for Service Accounts.
                    type: boolean
                  encryptionProviderKey:
                    description: |-
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                      The ID of the Key Management Service (KMS) key that is used to encrypt Kubernetes Secrets.
                    type: string
                  id:
                    description: The ID of the container cluster.
                    type: string
                  ipStack:
                    description: 'The IP address family that the cluster network uses.
                      Valid values:'
                    type: string
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. Only works for Create Operation. See Advanced security
                      group.'
                    type: boolean
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  maintenanceWindow:
                    description: The cluster maintenance window. Managed node pool
                      will use it. See maintenance_window below.
                    items:
                      properties:
                        duration:
                          description: 'The maintenance time, values range from 1
                            to 24,unit is hour. For example: "3h".'
                          type: string
                        enable:
                          description: Whether to open the maintenance window. The
                            following parameters take effect only enable = true.
                          type: boolean
                        maintenanceTime:
                          description: 'Initial maintenance time, RFC3339 format.
                            For example: "2024-10-15T12:31:00.000+08:00".'
                          type: string
                        weeklyPeriod:
                          description: Maintenance cycle, you can set the values from
                            Monday to Sunday, separated by commas when the values
                            are multiple. The default is Thursday.
                          type: string
                      type: object
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  natGatewayId:
                    description: The ID of nat gateway used to launch kubernetes cluster.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Only works for Create Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  operationPolicy:
                    description: The cluster automatic operation policy, only works
                      when maintenance_window is enabled. See operation_policy below.
                    items:
                      properties:
                        clusterAutoUpgrade:
                          description: Automatic cluster upgrade policy. See cluster_auto_upgrade
                            below.
                          items:
                            properties:
                              channel:
                                description: 'The automatic cluster upgrade channel.
                                  Valid values: patch, stable, rapid.'
                                type: string
                              enabled:
                                description: Whether to enable automatic cluster upgrade.
                                type: boolean
                            type: object
                          type: array
                      type: object
                    type: array
                  podCidr:
                    description: '- [Flannel Specific] The CIDR block for the pod
                      network when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to vswitch_ids but must be in same availability zones.
                      Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  profile:
                    description: 'The profile of cluster. Valid values:'
                    type: string
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  rrsaMetadata:
                    description: Nested attribute containing RRSA related data for
                      your cluster.
                    items:
                      properties:
                        enabled:
                          description: Whether to enable automatic cluster upgrade.
                          type: boolean
                        ramOidcProviderArn:
                          description: The arn of OIDC provider that was registered
                            in RAM.
                          type: string
                        ramOidcProviderName:
                          description: The name of OIDC Provider that was registered
                            in RAM.
                          type: string
                        rrsaOidcIssuerUrl:
                          description: The issuer URL of RRSA OIDC Token.
                          type: string
                      type: object
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22, Service Account
                      Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority. For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbId:
                    description: The ID of APIServer load balancer.
                    type: string
                  slbInternet:
                    description: The public ip of load balancer.
                    type: string
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  slbIntranet:
                    description: The ID of private load balancer where the current
                      cluster master node is located.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: Cluster timezone, works for control plane and Worker
                      nodes.
                    type: string
                  upgradePolicy:
                    description: Configuration block for cluster upgrade operations.
                      See upgrade_policy below.
                    items:
                      properties:
                        controlPlaneOnly:
                          description: 'Whether to upgrade only the control plane
                            without upgrading worker nodes. Valid values: true, false.
                            When set to true, only the cluster control plane components
                            will be upgraded, and worker nodes will remain at their
                            current version. Default is false.'
                          type: boolean
                      type: object
                    type: array
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                      Do not specify if cluster auto upgrade is enabled, see cluster_auto_upgrade
                      for more information.
                    type: string
                  vpcId:
                    description: The ID of VPC where the current cluster is located.
                    type: string
                  vswitchIds:
                    description: The vSwitches of the control plane.
                    items:
                      type: string
                    type: array
                  workerRamRoleName:
                    description: The RamRole Name attached to worker node.
                    type: string
                  workerVswitchIds:
                    description: The vSwitches used by control plane. Modification
                      after creation will not take effect. Please use vswitch_ids
                      to managed control plane vSwitches, which supports modifying
                      control plane vSwitches.
                    items:
                      type: string
                    type: array
                  zoneIds:
                    description: The IDs of the zone in which the cluster control
                      plane is deployed. ACK automatically creates a VPC in the region
                      and vSwitches in the specified zones. Only works for Create
                      Operation. Do not specify this with vswitch_ids together.
                    items:
                      type: string
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
