---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubernetes.ack.alibabacloud.m.crossplane.io
spec:
  group: ack.alibabacloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: Kubernetes
    listKind: KubernetesList
    plural: kubernetes
    singular: kubernetes
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Kubernetes is the Schema for the Kubernetess API. Provides a
          Alicloud resource to manage container kubernetes cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesSpec defines the desired state of Kubernetes
            properties:
              forProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: The version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature requires
                      specifying service_account_issuer as well. From cluster version
                      1.22+, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableSsh:
                    description: Enable login to the node through SSH. Default to
                      false.
                    type: boolean
                  imageId:
                    description: Custom Image support. Must be based on AliyunLinux
                      or AliyunLinux3.
                    type: string
                  installCloudMonitor:
                    description: Install cloud monitor agent on ECS. Default to true.
                    type: boolean
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. See Advanced security group. Only works for Create Operation.'
                    type: boolean
                  keyName:
                    description: The keypair of ssh login cluster node, you have to
                      create it first. You have to specify one of password key_name
                      kms_encrypted_password fields.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a cs kubernetes.
                      You have to specify one of password key_name kms_encrypted_password
                      fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a cs kubernetes with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  masterAutoRenew:
                    description: Enable master payment auto-renew, defaults to false.
                    type: boolean
                  masterAutoRenewPeriod:
                    description: Master payment auto-renew period, it can be one of
                      {1, 2, 3, 6, 12}.
                    type: number
                  masterDiskCategory:
                    description: The system disk category of master node. Its valid
                      value are cloud_ssd, cloud_essd and cloud_efficiency. Default
                      to cloud_efficiency.
                    type: string
                  masterDiskPerformanceLevel:
                    description: Master node system disk performance level. When master_disk_category
                      values cloud_essd, the optional values are PL0, PL1, PL2 or
                      PL3, but the specific performance level is related to the disk
                      capacity. For more information, see Enhanced SSDs. Default is
                      PL1.
                    type: string
                  masterDiskSize:
                    description: The system disk size of master node. Its valid value
                      range [20~500] in GB. Default to 20.
                    type: number
                  masterDiskSnapshotPolicyId:
                    description: Master node system disk auto snapshot policy.
                    type: string
                  masterInstanceChargeType:
                    description: Master payment type. or PostPaid or PrePaid, defaults
                      to PostPaid. If value is PrePaid, the files master_period, master_period_unit,
                      master_auto_renew and master_auto_renew_period are required.
                    type: string
                  masterInstanceTypes:
                    description: The instance type of master node. Specify one type
                      for single AZ Cluster, three types for MultiAZ Cluster.
                    items:
                      type: string
                    type: array
                  masterPeriod:
                    description: Master payment period.Its valid value is one of {1,
                      2, 3, 6, 12, 24, 36, 48, 60}.
                    type: number
                  masterPeriodUnit:
                    description: Master payment period unit, the valid value is Month.
                    type: string
                  masterVswitchIds:
                    description: The vswitches used by master, you can specific 3
                      or 5 vswitches because of the amount of masters. Detailed below.
                    items:
                      type: string
                    type: array
                  masterVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate masterVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  masterVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      masterVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Your cluster nodes and applications will have public
                      network access. If there is a NAT gateway in the selected VPC,
                      ACK will use this gateway by default; if there is no NAT gateway
                      in the selected VPC, ACK will create a new NAT gateway for you
                      and automatically configure SNAT rules. Only works for Create
                      Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: Each node name consists of a prefix, an IP substring,
                      and a suffix, the input format is customized,<prefix>,IPSubStringLen,<suffix>.
                      For example "customized,aliyun.com-,5,-test", if the node IP
                      address is 192.168.59.176, the prefix is aliyun.com-, IP substring
                      length is 5, and the suffix is -test, the node name will be
                      aliyun.com-59176-test.
                    type: string
                  osType:
                    description: The operating system of the nodes that run pods,
                      its valid value is either Linux or Windows. Default to Linux.
                    type: string
                  passwordSecretRef:
                    description: The password of ssh login cluster node. You have
                      to specify one of password key_name kms_encrypted_password fields.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  platform:
                    description: The architecture of the nodes that run pods, its
                      valid value AliyunLinux, AliyunLinux3. Default to AliyunLinux3.
                    type: string
                  podCidr:
                    description: '[Flannel Specific] The CIDR block for the pod network
                      when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to worker_vswitch_ids and master_vswitch_ids but
                      must be in same availability zones. Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  podVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate podVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  podVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      podVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  rdsInstances:
                    description: RDS instance list, You can choose which RDS instances
                      whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  runtime:
                    description: A list of runtime containers. If you select another
                      container runtime, see How do I select between Docker and Sandboxed-Container.
                      See runtime below.
                    items:
                      properties:
                        name:
                          description: The name of the runtime. Supported runtimes
                            can be queried by data source alicloud_cs_kubernetes_version.
                          type: string
                        version:
                          description: The version of the runtime.
                          type: string
                      type: object
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22+, Service
                      Account Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority.For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: When you create a cluster, set the time zones for
                      the Master and Worker nodes. You can only change the managed
                      node time zone if you create a cluster. Once the cluster is
                      created, you can only change the time zone of the Worker node.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: The version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature requires
                      specifying service_account_issuer as well. From cluster version
                      1.22+, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableSsh:
                    description: Enable login to the node through SSH. Default to
                      false.
                    type: boolean
                  imageId:
                    description: Custom Image support. Must be based on AliyunLinux
                      or AliyunLinux3.
                    type: string
                  installCloudMonitor:
                    description: Install cloud monitor agent on ECS. Default to true.
                    type: boolean
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. See Advanced security group. Only works for Create Operation.'
                    type: boolean
                  keyName:
                    description: The keypair of ssh login cluster node, you have to
                      create it first. You have to specify one of password key_name
                      kms_encrypted_password fields.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a cs kubernetes.
                      You have to specify one of password key_name kms_encrypted_password
                      fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a cs kubernetes with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  masterAutoRenew:
                    description: Enable master payment auto-renew, defaults to false.
                    type: boolean
                  masterAutoRenewPeriod:
                    description: Master payment auto-renew period, it can be one of
                      {1, 2, 3, 6, 12}.
                    type: number
                  masterDiskCategory:
                    description: The system disk category of master node. Its valid
                      value are cloud_ssd, cloud_essd and cloud_efficiency. Default
                      to cloud_efficiency.
                    type: string
                  masterDiskPerformanceLevel:
                    description: Master node system disk performance level. When master_disk_category
                      values cloud_essd, the optional values are PL0, PL1, PL2 or
                      PL3, but the specific performance level is related to the disk
                      capacity. For more information, see Enhanced SSDs. Default is
                      PL1.
                    type: string
                  masterDiskSize:
                    description: The system disk size of master node. Its valid value
                      range [20~500] in GB. Default to 20.
                    type: number
                  masterDiskSnapshotPolicyId:
                    description: Master node system disk auto snapshot policy.
                    type: string
                  masterInstanceChargeType:
                    description: Master payment type. or PostPaid or PrePaid, defaults
                      to PostPaid. If value is PrePaid, the files master_period, master_period_unit,
                      master_auto_renew and master_auto_renew_period are required.
                    type: string
                  masterInstanceTypes:
                    description: The instance type of master node. Specify one type
                      for single AZ Cluster, three types for MultiAZ Cluster.
                    items:
                      type: string
                    type: array
                  masterPeriod:
                    description: Master payment period.Its valid value is one of {1,
                      2, 3, 6, 12, 24, 36, 48, 60}.
                    type: number
                  masterPeriodUnit:
                    description: Master payment period unit, the valid value is Month.
                    type: string
                  masterVswitchIds:
                    description: The vswitches used by master, you can specific 3
                      or 5 vswitches because of the amount of masters. Detailed below.
                    items:
                      type: string
                    type: array
                  masterVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate masterVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  masterVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      masterVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Your cluster nodes and applications will have public
                      network access. If there is a NAT gateway in the selected VPC,
                      ACK will use this gateway by default; if there is no NAT gateway
                      in the selected VPC, ACK will create a new NAT gateway for you
                      and automatically configure SNAT rules. Only works for Create
                      Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: Each node name consists of a prefix, an IP substring,
                      and a suffix, the input format is customized,<prefix>,IPSubStringLen,<suffix>.
                      For example "customized,aliyun.com-,5,-test", if the node IP
                      address is 192.168.59.176, the prefix is aliyun.com-, IP substring
                      length is 5, and the suffix is -test, the node name will be
                      aliyun.com-59176-test.
                    type: string
                  osType:
                    description: The operating system of the nodes that run pods,
                      its valid value is either Linux or Windows. Default to Linux.
                    type: string
                  passwordSecretRef:
                    description: The password of ssh login cluster node. You have
                      to specify one of password key_name kms_encrypted_password fields.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  platform:
                    description: The architecture of the nodes that run pods, its
                      valid value AliyunLinux, AliyunLinux3. Default to AliyunLinux3.
                    type: string
                  podCidr:
                    description: '[Flannel Specific] The CIDR block for the pod network
                      when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to worker_vswitch_ids and master_vswitch_ids but
                      must be in same availability zones. Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  podVswitchIdsRefs:
                    description: References to Vswitch in vpc to populate podVswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  podVswitchIdsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      podVswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  rdsInstances:
                    description: RDS instance list, You can choose which RDS instances
                      whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  runtime:
                    description: A list of runtime containers. If you select another
                      container runtime, see How do I select between Docker and Sandboxed-Container.
                      See runtime below.
                    items:
                      properties:
                        name:
                          description: The name of the runtime. Supported runtimes
                            can be queried by data source alicloud_cs_kubernetes_version.
                          type: string
                        version:
                          description: The version of the runtime.
                          type: string
                      type: object
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22+, Service
                      Account Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority.For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: When you create a cluster, set the time zones for
                      the Master and Worker nodes. You can only change the managed
                      node time zone if you create a cluster. Once the cluster is
                      created, you can only change the time zone of the Worker node.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.masterInstanceTypes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.masterInstanceTypes)
                || (has(self.initProvider) && has(self.initProvider.masterInstanceTypes))'
          status:
            description: KubernetesStatus defines the observed state of Kubernetes.
            properties:
              atProvider:
                properties:
                  addons:
                    description: The addon you want to install in cluster. See addons
                      below. Only works for Create Operation, use resource cs_kubernetes_addon
                      to manage addons if cluster is created.
                    items:
                      properties:
                        config:
                          description: The ACK add-on configurations. For more config
                            information, see cs_kubernetes_addon_metadata.
                          type: string
                        disabled:
                          description: Disables the automatic installation of a component.
                            Default is false.
                          type: boolean
                        name:
                          description: Name of the ACK add-on. The name must match
                            one of the names returned by DescribeAddons.
                          type: string
                        version:
                          description: The version of the component.
                          type: string
                      type: object
                    type: array
                  apiAudiences:
                    description: A list of API audiences for Service Account Token
                      Volume Projection. Set this to ["https://kubernetes.default.svc"]
                      if you want to enable the Token Volume Projection feature requires
                      specifying service_account_issuer as well. From cluster version
                      1.22+, Service Account Token Volume Projection will be enabled
                      by default.
                    items:
                      type: string
                    type: array
                  certificateAuthority:
                    description: (List, Deprecated from v1.248.0) Nested attribute
                      containing certificate authority data for your cluster. Please
                      use the attribute certificate_authority of new DataSource alicloud_cs_cluster_credential
                      to replace it.
                    items:
                      properties:
                        clientCert:
                          description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                            is recommended to manage cluster's kubeconfig, you can
                            also save the certificate_authority.client_cert attribute
                            content of new DataSource alicloud_cs_cluster_credential
                            to an appropriate path(like ~/.kube/client-cert.pem) for
                            replace it.
                          type: string
                        clientKey:
                          description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                            is recommended to manage cluster's kubeconfig, you can
                            also save the certificate_authority.client_key attribute
                            content of new DataSource alicloud_cs_cluster_credential
                            to an appropriate path(like ~/.kube/client-key.pem) for
                            replace it.
                          type: string
                        clusterCert:
                          description: The base64 encoded cluster certificate data
                            required to communicate with your cluster. Add this to
                            the certificate-authority-data section of the kubeconfig
                            file for your cluster.
                          type: string
                      type: object
                    type: array
                  clientCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-cert.pem) for replace it.
                    type: string
                  clientKey:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.client_key attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/client-key.pem) for replace it.
                    type: string
                  clusterCaCert:
                    description: From version 1.248.0, new DataSource alicloud_cs_cluster_credential
                      is recommended to manage cluster's kubeconfig, you can also
                      save the certificate_authority.cluster_cert attribute content
                      of new DataSource alicloud_cs_cluster_credential to an appropriate
                      path(like ~/.kube/cluster-ca-cert.pem) for replace it.
                    type: string
                  clusterDomain:
                    description: |-
                      Cluster local domain name, Default to cluster.local. A domain name consists of one or more sections separated by a decimal point (.), each of which is up to 63 characters long, and can be lowercase, numerals, and underscores (-), and must be lowercase or numerals at the beginning and end.
                      cluster local domain
                    type: string
                  connections:
                    description: (List) A list of kubernetes cluster connection information.
                    items:
                      properties:
                        apiServerInternet:
                          description: API Server Internet endpoint.
                          type: string
                        apiServerIntranet:
                          description: API Server Intranet endpoint.
                          type: string
                        masterPublicIp:
                          description: Master node SSH IP address.
                          type: string
                        serviceDomain:
                          description: Service Access Domain.
                          type: string
                      type: object
                    type: array
                  customSan:
                    description: Customize the certificate SAN, multiple IP or domain
                      names are separated by English commas (,).
                    type: string
                  deleteOptions:
                    description: Delete options, only work for deleting resource.
                      See delete_options below.
                    items:
                      properties:
                        deleteMode:
                          description: 'The deletion mode of the cluster. Different
                            resources may have different default behavior, see resource_type
                            for details. Valid values:'
                          type: string
                        resourceType:
                          description: 'The type of resources that are created by
                            cluster. Valid values:'
                          type: string
                      type: object
                    type: array
                  deletionProtection:
                    description: Whether to enable cluster deletion protection.
                    type: boolean
                  enableSsh:
                    description: Enable login to the node through SSH. Default to
                      false.
                    type: boolean
                  id:
                    description: The ID of the container cluster.
                    type: string
                  imageId:
                    description: Custom Image support. Must be based on AliyunLinux
                      or AliyunLinux3.
                    type: string
                  installCloudMonitor:
                    description: Install cloud monitor agent on ECS. Default to true.
                    type: boolean
                  isEnterpriseSecurityGroup:
                    description: 'Enable to create advanced security group. default:
                      false. See Advanced security group. Only works for Create Operation.'
                    type: boolean
                  keyName:
                    description: The keypair of ssh login cluster node, you have to
                      create it first. You have to specify one of password key_name
                      kms_encrypted_password fields.
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to a cs kubernetes.
                      You have to specify one of password key_name kms_encrypted_password
                      fields.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating a cs kubernetes with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  loadBalancerSpec:
                    description: The cluster api server load balancer instance specification.
                      For more information on how to select a LB instance specification,
                      see SLB instance overview. Only works for Create Operation.
                      The spec will not take effect because the charge of the load
                      balancer has been changed to PayByCLCU.
                    type: string
                  masterAutoRenew:
                    description: Enable master payment auto-renew, defaults to false.
                    type: boolean
                  masterAutoRenewPeriod:
                    description: Master payment auto-renew period, it can be one of
                      {1, 2, 3, 6, 12}.
                    type: number
                  masterDiskCategory:
                    description: The system disk category of master node. Its valid
                      value are cloud_ssd, cloud_essd and cloud_efficiency. Default
                      to cloud_efficiency.
                    type: string
                  masterDiskPerformanceLevel:
                    description: Master node system disk performance level. When master_disk_category
                      values cloud_essd, the optional values are PL0, PL1, PL2 or
                      PL3, but the specific performance level is related to the disk
                      capacity. For more information, see Enhanced SSDs. Default is
                      PL1.
                    type: string
                  masterDiskSize:
                    description: The system disk size of master node. Its valid value
                      range [20~500] in GB. Default to 20.
                    type: number
                  masterDiskSnapshotPolicyId:
                    description: Master node system disk auto snapshot policy.
                    type: string
                  masterInstanceChargeType:
                    description: Master payment type. or PostPaid or PrePaid, defaults
                      to PostPaid. If value is PrePaid, the files master_period, master_period_unit,
                      master_auto_renew and master_auto_renew_period are required.
                    type: string
                  masterInstanceTypes:
                    description: The instance type of master node. Specify one type
                      for single AZ Cluster, three types for MultiAZ Cluster.
                    items:
                      type: string
                    type: array
                  masterNodes:
                    description: The master nodes. See master_nodes below.
                    items:
                      properties:
                        id:
                          description: The id of a node.
                          type: string
                        name:
                          description: The name of a node.
                          type: string
                        privateIp:
                          description: The private ip of a node.
                          type: string
                      type: object
                    type: array
                  masterPeriod:
                    description: Master payment period.Its valid value is one of {1,
                      2, 3, 6, 12, 24, 36, 48, 60}.
                    type: number
                  masterPeriodUnit:
                    description: Master payment period unit, the valid value is Month.
                    type: string
                  masterVswitchIds:
                    description: The vswitches used by master, you can specific 3
                      or 5 vswitches because of the amount of masters. Detailed below.
                    items:
                      type: string
                    type: array
                  name:
                    description: The kubernetes cluster's name. It is unique in one
                      Alicloud account.
                    type: string
                  natGatewayId:
                    description: The ID of nat gateway used to launch kubernetes cluster.
                    type: string
                  newNatGateway:
                    description: Whether to create a new nat gateway while creating
                      kubernetes cluster. Default to true. Then openapi in Alibaba
                      Cloud are not all on intranet, So turn this option on is a good
                      choice. Your cluster nodes and applications will have public
                      network access. If there is a NAT gateway in the selected VPC,
                      ACK will use this gateway by default; if there is no NAT gateway
                      in the selected VPC, ACK will create a new NAT gateway for you
                      and automatically configure SNAT rules. Only works for Create
                      Operation.
                    type: boolean
                  nodeCidrMask:
                    description: 'The node cidr block to specific how many pods can
                      run on single node. 24-28 is allowed. 24 means 2^(32-24)-1=255
                      and the node can run at most 255 pods. default: 24'
                    type: number
                  nodeNameMode:
                    description: Each node name consists of a prefix, an IP substring,
                      and a suffix, the input format is customized,<prefix>,IPSubStringLen,<suffix>.
                      For example "customized,aliyun.com-,5,-test", if the node IP
                      address is 192.168.59.176, the prefix is aliyun.com-, IP substring
                      length is 5, and the suffix is -test, the node name will be
                      aliyun.com-59176-test.
                    type: string
                  osType:
                    description: The operating system of the nodes that run pods,
                      its valid value is either Linux or Windows. Default to Linux.
                    type: string
                  platform:
                    description: The architecture of the nodes that run pods, its
                      valid value AliyunLinux, AliyunLinux3. Default to AliyunLinux3.
                    type: string
                  podCidr:
                    description: '[Flannel Specific] The CIDR block for the pod network
                      when using Flannel.'
                    type: string
                  podVswitchIds:
                    description: '- [Terway Specific] The vswitches for the pod network
                      when using Terway. It is recommended that pod_vswitch_ids is
                      not belong to worker_vswitch_ids and master_vswitch_ids but
                      must be in same availability zones. Only works for Create Operation.'
                    items:
                      type: string
                    type: array
                  proxyMode:
                    description: 'kube-proxy proxy mode. Default: ipvs. Options: iptables,
                      ipvs, nftables.'
                    type: string
                  rdsInstances:
                    description: RDS instance list, You can choose which RDS instances
                      whitelist to add instances to.
                    items:
                      type: string
                    type: array
                  resourceGroupId:
                    description: The ID of the resource group,by default these cloud
                      resources are automatically assigned to the default resource
                      group.
                    type: string
                  retainResources:
                    description: Resources that are automatically created during cluster
                      creation, including NAT gateways, SNAT rules, SLB instances,
                      and RAM Role, will be deleted. Resources that are manually created
                      after you create the cluster, such as SLB instances for Services,
                      will also be deleted. If you need to retain resources, please
                      configure with retain_resources. There are several aspects to
                      pay attention to when using retain_resources to retain resources.
                    items:
                      type: string
                    type: array
                  runtime:
                    description: A list of runtime containers. If you select another
                      container runtime, see How do I select between Docker and Sandboxed-Container.
                      See runtime below.
                    items:
                      properties:
                        name:
                          description: The name of the runtime. Supported runtimes
                            can be queried by data source alicloud_cs_kubernetes_version.
                          type: string
                        version:
                          description: The version of the runtime.
                          type: string
                      type: object
                    type: array
                  securityGroupId:
                    description: The ID of the security group to which the ECS instances
                      in the cluster belong. If it is not specified, a new Security
                      group will be built.
                    type: string
                  serviceAccountIssuer:
                    description: The issuer of the Service Account token for Service
                      Account Token Volume Projection, corresponds to the iss field
                      in the token payload. Set this to "https://kubernetes.default.svc"
                      to enable the Token Volume Projection feature (requires specifying
                      api_audiences as well). From cluster version 1.22+, Service
                      Account Token Volume Projection will be enabled by default.
                    type: string
                  serviceCidr:
                    description: The CIDR block for the service network. It cannot
                      be duplicated with the VPC CIDR and CIDR used by Kubernetes
                      cluster in VPC, cannot be modified after creation.
                    type: string
                  skipSetCertificateAuthority:
                    description: Configure whether to save certificate authority data
                      for your cluster to attribute certificate_authority.For cluster
                      security, recommended configuration as true. Will be removed
                      with attribute certificate_authority removed.
                    type: boolean
                  slbId:
                    description: The ID of APIServer load balancer.
                    type: string
                  slbInternet:
                    description: The public ip of load balancer.
                    type: string
                  slbInternetEnabled:
                    description: Whether to create internet load balancer for API
                      Server. Default to true. Only works for Create Operation.
                    type: boolean
                  slbIntranet:
                    description: The ID of private load balancer where the current
                      cluster master node is located.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  timezone:
                    description: When you create a cluster, set the time zones for
                      the Master and Worker nodes. You can only change the managed
                      node time zone if you create a cluster. Once the cluster is
                      created, you can only change the time zone of the Worker node.
                    type: string
                  userCa:
                    description: The path of customized CA cert, you can use this
                      CA to sign client certs to connect your cluster.
                    type: string
                  version:
                    description: Desired Kubernetes version. If you do not specify
                      a value, the latest available version at resource creation is
                      used and no upgrades will occur except you set a higher version
                      number. The value must be configured and increased to upgrade
                      the version when desired. Downgrades are not supported by ACK.
                    type: string
                  vpcId:
                    description: The ID of VPC where the current cluster is located.
                    type: string
                  workerRamRoleName:
                    description: The RamRole Name attached to worker node.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
