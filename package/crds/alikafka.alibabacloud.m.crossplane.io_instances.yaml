---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: instances.alikafka.alibabacloud.m.crossplane.io
spec:
  group: alikafka.alibabacloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: Instance
    listKind: InstanceList
    plural: instances
    singular: instance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Instance is the Schema for the Instances API. Provides a Alicloud
          AliKafka Instance resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceSpec defines the desired state of Instance
            properties:
              forProvider:
                properties:
                  config:
                    description: The initial configurations of the ApsaraMQ for Kafka
                      instance. The values must be valid JSON strings.
                    type: string
                  confluentConfig:
                    description: |-
                      The configurations of Confluent. See confluent_config below.
                      -> NOTE: If instance_type is set to alikafka_confluent, confluent_config is required.
                    items:
                      properties:
                        connectCu:
                          description: The number of CPU cores of Connect.
                          type: number
                        connectReplica:
                          description: The number of replicas of Connect.
                          type: number
                        controlCenterCu:
                          description: The number of CPU cores of Control Center.
                          type: number
                        controlCenterReplica:
                          description: The number of replicas of Control Center.
                          type: number
                        controlCenterStorage:
                          description: The disk capacity of Control Center.
                          type: number
                        kafkaCu:
                          description: The number of CPU cores of the Kafka broker.
                          type: number
                        kafkaReplica:
                          description: The number of replicas of the Kafka broker.
                          type: number
                        kafkaRestProxyCu:
                          description: The number of CPU cores of Kafka Rest Proxy.
                          type: number
                        kafkaRestProxyReplica:
                          description: The number of replicas of Kafka Rest Proxy.
                          type: number
                        kafkaStorage:
                          description: The disk capacity of the Kafka broker.
                          type: number
                        ksqlCu:
                          description: The number of CPU cores of ksqlDB.
                          type: number
                        ksqlReplica:
                          description: The number of replicas of ksqlDB.
                          type: number
                        ksqlStorage:
                          description: The disk capacity of ksqlDB.
                          type: number
                        schemaRegistryCu:
                          description: The number of CPU cores of Schema Registry.
                          type: number
                        schemaRegistryReplica:
                          description: The number of replicas of Schema Registry.
                          type: number
                        zookeeperCu:
                          description: The number of CPU cores of ZooKeeper.
                          type: number
                        zookeeperReplica:
                          description: The number of replicas of ZooKeeper.
                          type: number
                        zookeeperStorage:
                          description: The disk capacity of ZooKeeper.
                          type: number
                      type: object
                    type: array
                  defaultTopicPartitionNum:
                    description: The number of partitions in a topic that is automatically
                      created.
                    type: number
                  deployType:
                    description: 'The deployment type of the instance. NOTE: From
                      version 1.161.0, this attribute supports to be updated. Valid
                      values:'
                    type: number
                  diskSize:
                    description: |-
                      The disk size of the instance. When modify this value, it only supports adjust to a greater value.
                      -> NOTE: If instance_type is set to alikafka, disk_size is required.
                    type: number
                  diskType:
                    description: 'The disk type of the instance. Valid values:'
                    type: number
                  eipMax:
                    description: The max bandwidth of the instance. It will be ignored
                      when deploy_type = 5. When modify this value, it only supports
                      adjust to a greater value.
                    type: number
                  enableAutoGroup:
                    description: 'Specify whether to enable the flexible group creation
                      feature. Default value: false. Valid values:'
                    type: boolean
                  enableAutoTopic:
                    description: 'Specify whether to enable the automatic topic creation
                      feature. Default value: disable. Valid values:'
                    type: string
                  instanceType:
                    description: 'The type of the Instance. Default value: alikafka.
                      Valid values:'
                    type: string
                  ioMax:
                    description: The max value of io of the instance. When modify
                      this value, it only support adjust to a greater value.
                    type: number
                  ioMaxSpec:
                    description: The traffic specification of the instance. We recommend
                      that you configure this parameter.
                    type: string
                  kmsKeyId:
                    description: The ID of the key that is used to encrypt data on
                      standard SSDs in the region of the instance. For more information,
                      see How to use it.
                    type: string
                  kmsKeyIdRef:
                    description: Reference to a Key in kms to populate kmsKeyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  kmsKeyIdSelector:
                    description: Selector for a Key in kms to populate kmsKeyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: Name of your Kafka instance. The length should between
                      3 and 64 characters. If not set, will use instance id as instance
                      name.
                    type: string
                  paidType:
                    description: 'The billing method of the instance. Default value:
                      PostPaid. Valid values: PostPaid, PrePaid. When modify this
                      value, it only support adjust from PostPaid to PrePaid.'
                    type: string
                  partitionNum:
                    description: The number of partitions.
                    type: number
                  password:
                    description: 'The instance password. NOTE: If instance_type is
                      set to alikafka_confluent, password is required.'
                    type: string
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: 'The ID of the resource group. Note: Once you set
                      a value of this property, you cannot set it to an empty string
                      anymore.'
                    type: string
                  securityGroup:
                    description: The ID of security group for this instance. If the
                      security group is empty, system will create a default one.
                    type: string
                  securityGroupRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroup.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroup.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  selectedZones:
                    description: The zones among which you want to deploy the instance.
                      For more information, see How to use it.
                    items:
                      type: string
                    type: array
                  serverlessConfig:
                    description: |-
                      The parameters configured for the serverless ApsaraMQ for Kafka instance. See serverless_config below.
                      -> NOTE: If instance_type is set to alikafka_serverless, serverless_config is required.
                    items:
                      properties:
                        reservedPublishCapacity:
                          description: The reserved capacity for publishing messages.
                            The minimum value is 60.
                          type: number
                        reservedSubscribeCapacity:
                          description: The reserved capacity for subscribing to message.
                            The minimum value is 20.
                          type: number
                      type: object
                    type: array
                  serviceVersion:
                    description: 'The version of the Instance. Valid values:'
                    type: string
                  specType:
                    description: 'The instance edition. Default value: normal. Valid
                      values:'
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The VPC ID of the instance.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIDsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIDsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: The ID of attaching vswitch to instance.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIds:
                    description: |-
                      The IDs of the vSwitches with which the instance is associated.
                      -> NOTE: If instance_type is set to alikafka or alikafka_serverless, vswitch_ids is required. When instance_type is set to alikafka_confluent, you must specify at least one of the vswitch_id and vswitch_ids, and if you specify both vswitch_id and vswitch_ids, only the vswitch_ids takes effect.
                    items:
                      type: string
                    type: array
                  zoneId:
                    description: 'The zone ID of the instance. The value can be in
                      zone x or region id-x format. NOTE: When the available zone
                      is insufficient, another availability zone may be deployed.'
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  config:
                    description: The initial configurations of the ApsaraMQ for Kafka
                      instance. The values must be valid JSON strings.
                    type: string
                  confluentConfig:
                    description: |-
                      The configurations of Confluent. See confluent_config below.
                      -> NOTE: If instance_type is set to alikafka_confluent, confluent_config is required.
                    items:
                      properties:
                        connectCu:
                          description: The number of CPU cores of Connect.
                          type: number
                        connectReplica:
                          description: The number of replicas of Connect.
                          type: number
                        controlCenterCu:
                          description: The number of CPU cores of Control Center.
                          type: number
                        controlCenterReplica:
                          description: The number of replicas of Control Center.
                          type: number
                        controlCenterStorage:
                          description: The disk capacity of Control Center.
                          type: number
                        kafkaCu:
                          description: The number of CPU cores of the Kafka broker.
                          type: number
                        kafkaReplica:
                          description: The number of replicas of the Kafka broker.
                          type: number
                        kafkaRestProxyCu:
                          description: The number of CPU cores of Kafka Rest Proxy.
                          type: number
                        kafkaRestProxyReplica:
                          description: The number of replicas of Kafka Rest Proxy.
                          type: number
                        kafkaStorage:
                          description: The disk capacity of the Kafka broker.
                          type: number
                        ksqlCu:
                          description: The number of CPU cores of ksqlDB.
                          type: number
                        ksqlReplica:
                          description: The number of replicas of ksqlDB.
                          type: number
                        ksqlStorage:
                          description: The disk capacity of ksqlDB.
                          type: number
                        schemaRegistryCu:
                          description: The number of CPU cores of Schema Registry.
                          type: number
                        schemaRegistryReplica:
                          description: The number of replicas of Schema Registry.
                          type: number
                        zookeeperCu:
                          description: The number of CPU cores of ZooKeeper.
                          type: number
                        zookeeperReplica:
                          description: The number of replicas of ZooKeeper.
                          type: number
                        zookeeperStorage:
                          description: The disk capacity of ZooKeeper.
                          type: number
                      type: object
                    type: array
                  defaultTopicPartitionNum:
                    description: The number of partitions in a topic that is automatically
                      created.
                    type: number
                  deployType:
                    description: 'The deployment type of the instance. NOTE: From
                      version 1.161.0, this attribute supports to be updated. Valid
                      values:'
                    type: number
                  diskSize:
                    description: |-
                      The disk size of the instance. When modify this value, it only supports adjust to a greater value.
                      -> NOTE: If instance_type is set to alikafka, disk_size is required.
                    type: number
                  diskType:
                    description: 'The disk type of the instance. Valid values:'
                    type: number
                  eipMax:
                    description: The max bandwidth of the instance. It will be ignored
                      when deploy_type = 5. When modify this value, it only supports
                      adjust to a greater value.
                    type: number
                  enableAutoGroup:
                    description: 'Specify whether to enable the flexible group creation
                      feature. Default value: false. Valid values:'
                    type: boolean
                  enableAutoTopic:
                    description: 'Specify whether to enable the automatic topic creation
                      feature. Default value: disable. Valid values:'
                    type: string
                  instanceType:
                    description: 'The type of the Instance. Default value: alikafka.
                      Valid values:'
                    type: string
                  ioMax:
                    description: The max value of io of the instance. When modify
                      this value, it only support adjust to a greater value.
                    type: number
                  ioMaxSpec:
                    description: The traffic specification of the instance. We recommend
                      that you configure this parameter.
                    type: string
                  kmsKeyId:
                    description: The ID of the key that is used to encrypt data on
                      standard SSDs in the region of the instance. For more information,
                      see How to use it.
                    type: string
                  kmsKeyIdRef:
                    description: Reference to a Key in kms to populate kmsKeyId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  kmsKeyIdSelector:
                    description: Selector for a Key in kms to populate kmsKeyId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  name:
                    description: Name of your Kafka instance. The length should between
                      3 and 64 characters. If not set, will use instance id as instance
                      name.
                    type: string
                  paidType:
                    description: 'The billing method of the instance. Default value:
                      PostPaid. Valid values: PostPaid, PrePaid. When modify this
                      value, it only support adjust from PostPaid to PrePaid.'
                    type: string
                  partitionNum:
                    description: The number of partitions.
                    type: number
                  password:
                    description: 'The instance password. NOTE: If instance_type is
                      set to alikafka_confluent, password is required.'
                    type: string
                  resourceGroupId:
                    description: 'The ID of the resource group. Note: Once you set
                      a value of this property, you cannot set it to an empty string
                      anymore.'
                    type: string
                  securityGroup:
                    description: The ID of security group for this instance. If the
                      security group is empty, system will create a default one.
                    type: string
                  securityGroupRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroup.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroup.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  selectedZones:
                    description: The zones among which you want to deploy the instance.
                      For more information, see How to use it.
                    items:
                      type: string
                    type: array
                  serverlessConfig:
                    description: |-
                      The parameters configured for the serverless ApsaraMQ for Kafka instance. See serverless_config below.
                      -> NOTE: If instance_type is set to alikafka_serverless, serverless_config is required.
                    items:
                      properties:
                        reservedPublishCapacity:
                          description: The reserved capacity for publishing messages.
                            The minimum value is 60.
                          type: number
                        reservedSubscribeCapacity:
                          description: The reserved capacity for subscribing to message.
                            The minimum value is 20.
                          type: number
                      type: object
                    type: array
                  serviceVersion:
                    description: 'The version of the Instance. Valid values:'
                    type: string
                  specType:
                    description: 'The instance edition. Default value: normal. Valid
                      values:'
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The VPC ID of the instance.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIDsRefs:
                    description: References to Vswitch in vpc to populate vswitchIds.
                    items:
                      description: A NamespacedReference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        namespace:
                          description: Namespace of the referenced object
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  vswitchIDsSelector:
                    description: Selector for a list of Vswitch in vpc to populate
                      vswitchIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchId:
                    description: The ID of attaching vswitch to instance.
                    type: string
                  vswitchIdRef:
                    description: Reference to a Vswitch in vpc to populate vswitchId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vswitchIdSelector:
                    description: Selector for a Vswitch in vpc to populate vswitchId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  vswitchIds:
                    description: |-
                      The IDs of the vSwitches with which the instance is associated.
                      -> NOTE: If instance_type is set to alikafka or alikafka_serverless, vswitch_ids is required. When instance_type is set to alikafka_confluent, you must specify at least one of the vswitch_id and vswitch_ids, and if you specify both vswitch_id and vswitch_ids, only the vswitch_ids takes effect.
                    items:
                      type: string
                    type: array
                  zoneId:
                    description: 'The zone ID of the instance. The value can be in
                      zone x or region id-x format. NOTE: When the available zone
                      is insufficient, another availability zone may be deployed.'
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.deployType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.deployType)
                || (has(self.initProvider) && has(self.initProvider.deployType))'
          status:
            description: InstanceStatus defines the observed state of Instance.
            properties:
              atProvider:
                properties:
                  config:
                    description: The initial configurations of the ApsaraMQ for Kafka
                      instance. The values must be valid JSON strings.
                    type: string
                  confluentConfig:
                    description: |-
                      The configurations of Confluent. See confluent_config below.
                      -> NOTE: If instance_type is set to alikafka_confluent, confluent_config is required.
                    items:
                      properties:
                        connectCu:
                          description: The number of CPU cores of Connect.
                          type: number
                        connectReplica:
                          description: The number of replicas of Connect.
                          type: number
                        controlCenterCu:
                          description: The number of CPU cores of Control Center.
                          type: number
                        controlCenterReplica:
                          description: The number of replicas of Control Center.
                          type: number
                        controlCenterStorage:
                          description: The disk capacity of Control Center.
                          type: number
                        kafkaCu:
                          description: The number of CPU cores of the Kafka broker.
                          type: number
                        kafkaReplica:
                          description: The number of replicas of the Kafka broker.
                          type: number
                        kafkaRestProxyCu:
                          description: The number of CPU cores of Kafka Rest Proxy.
                          type: number
                        kafkaRestProxyReplica:
                          description: The number of replicas of Kafka Rest Proxy.
                          type: number
                        kafkaStorage:
                          description: The disk capacity of the Kafka broker.
                          type: number
                        ksqlCu:
                          description: The number of CPU cores of ksqlDB.
                          type: number
                        ksqlReplica:
                          description: The number of replicas of ksqlDB.
                          type: number
                        ksqlStorage:
                          description: The disk capacity of ksqlDB.
                          type: number
                        schemaRegistryCu:
                          description: The number of CPU cores of Schema Registry.
                          type: number
                        schemaRegistryReplica:
                          description: The number of replicas of Schema Registry.
                          type: number
                        zookeeperCu:
                          description: The number of CPU cores of ZooKeeper.
                          type: number
                        zookeeperReplica:
                          description: The number of replicas of ZooKeeper.
                          type: number
                        zookeeperStorage:
                          description: The disk capacity of ZooKeeper.
                          type: number
                      type: object
                    type: array
                  defaultTopicPartitionNum:
                    description: The number of partitions in a topic that is automatically
                      created.
                    type: number
                  deployType:
                    description: 'The deployment type of the instance. NOTE: From
                      version 1.161.0, this attribute supports to be updated. Valid
                      values:'
                    type: number
                  diskSize:
                    description: |-
                      The disk size of the instance. When modify this value, it only supports adjust to a greater value.
                      -> NOTE: If instance_type is set to alikafka, disk_size is required.
                    type: number
                  diskType:
                    description: 'The disk type of the instance. Valid values:'
                    type: number
                  domainEndpoint:
                    description: (Available since v1.234.0) The default endpoint of
                      the instance in domain name mode.
                    type: string
                  eipMax:
                    description: The max bandwidth of the instance. It will be ignored
                      when deploy_type = 5. When modify this value, it only supports
                      adjust to a greater value.
                    type: number
                  enableAutoGroup:
                    description: 'Specify whether to enable the flexible group creation
                      feature. Default value: false. Valid values:'
                    type: boolean
                  enableAutoTopic:
                    description: 'Specify whether to enable the automatic topic creation
                      feature. Default value: disable. Valid values:'
                    type: string
                  endPoint:
                    description: The EndPoint to access the kafka instance.
                    type: string
                  groupLeft:
                    description: (Available since v1.214.1) The number of available
                      groups.
                    type: number
                  groupUsed:
                    description: (Available since v1.214.1) The number of used groups.
                    type: number
                  id:
                    type: string
                  instanceType:
                    description: 'The type of the Instance. Default value: alikafka.
                      Valid values:'
                    type: string
                  ioMax:
                    description: The max value of io of the instance. When modify
                      this value, it only support adjust to a greater value.
                    type: number
                  ioMaxSpec:
                    description: The traffic specification of the instance. We recommend
                      that you configure this parameter.
                    type: string
                  isPartitionBuy:
                    description: (Available since v1.214.1) The method that you use
                      to purchase partitions.
                    type: number
                  kmsKeyId:
                    description: The ID of the key that is used to encrypt data on
                      standard SSDs in the region of the instance. For more information,
                      see How to use it.
                    type: string
                  name:
                    description: Name of your Kafka instance. The length should between
                      3 and 64 characters. If not set, will use instance id as instance
                      name.
                    type: string
                  paidType:
                    description: 'The billing method of the instance. Default value:
                      PostPaid. Valid values: PostPaid, PrePaid. When modify this
                      value, it only support adjust from PostPaid to PrePaid.'
                    type: string
                  partitionLeft:
                    description: (Available since v1.214.1) The number of available
                      partitions.
                    type: number
                  partitionNum:
                    description: The number of partitions.
                    type: number
                  partitionUsed:
                    description: (Available since v1.214.1) The number of used partitions.
                    type: number
                  password:
                    description: 'The instance password. NOTE: If instance_type is
                      set to alikafka_confluent, password is required.'
                    type: string
                  resourceGroupId:
                    description: 'The ID of the resource group. Note: Once you set
                      a value of this property, you cannot set it to an empty string
                      anymore.'
                    type: string
                  saslDomainEndpoint:
                    description: (Available since v1.234.0) The Simple Authentication
                      and Security Layer (SASL) endpoint of the instance in domain
                      name mode.
                    type: string
                  securityGroup:
                    description: The ID of security group for this instance. If the
                      security group is empty, system will create a default one.
                    type: string
                  selectedZones:
                    description: The zones among which you want to deploy the instance.
                      For more information, see How to use it.
                    items:
                      type: string
                    type: array
                  serverlessConfig:
                    description: |-
                      The parameters configured for the serverless ApsaraMQ for Kafka instance. See serverless_config below.
                      -> NOTE: If instance_type is set to alikafka_serverless, serverless_config is required.
                    items:
                      properties:
                        reservedPublishCapacity:
                          description: The reserved capacity for publishing messages.
                            The minimum value is 60.
                          type: number
                        reservedSubscribeCapacity:
                          description: The reserved capacity for subscribing to message.
                            The minimum value is 20.
                          type: number
                      type: object
                    type: array
                  serviceVersion:
                    description: 'The version of the Instance. Valid values:'
                    type: string
                  specType:
                    description: 'The instance edition. Default value: normal. Valid
                      values:'
                    type: string
                  sslDomainEndpoint:
                    description: (Available since v1.234.0) The SSL endpoint of the
                      instance in domain name mode.
                    type: string
                  sslEndpoint:
                    description: (Available since v1.234.0) The Secure Sockets Layer
                      (SSL) endpoint of the instance in IP address mode.
                    type: string
                  status:
                    description: The status of the instance.
                    type: number
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  topicLeft:
                    description: (Available since v1.214.1) The number of available
                      topics.
                    type: number
                  topicNumOfBuy:
                    description: (Available since v1.214.1) The number of purchased
                      topics.
                    type: number
                  topicUsed:
                    description: (Available since v1.214.1) The number of used topics.
                    type: number
                  vpcId:
                    description: The VPC ID of the instance.
                    type: string
                  vpcSaslDomainEndpoint:
                    description: (Available since v1.283.0) The IP-based endpoint
                      for SASL access over a VPC.
                    type: string
                  vswitchId:
                    description: The ID of attaching vswitch to instance.
                    type: string
                  vswitchIds:
                    description: |-
                      The IDs of the vSwitches with which the instance is associated.
                      -> NOTE: If instance_type is set to alikafka or alikafka_serverless, vswitch_ids is required. When instance_type is set to alikafka_confluent, you must specify at least one of the vswitch_id and vswitch_ids, and if you specify both vswitch_id and vswitch_ids, only the vswitch_ids takes effect.
                    items:
                      type: string
                    type: array
                  zoneId:
                    description: 'The zone ID of the instance. The value can be in
                      zone x or region id-x format. NOTE: When the available zone
                      is insufficient, another availability zone may be deployed.'
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
