---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: securitygrouprules.ecs.alibabacloud.m.crossplane.io
spec:
  group: ecs.alibabacloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: SecurityGroupRule
    listKind: SecurityGroupRuleList
    plural: securitygrouprules
    singular: securitygrouprule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SecurityGroupRule is the Schema for the SecurityGroupRules API.
          Provides a Alicloud Security Group Rule resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityGroupRuleSpec defines the desired state of SecurityGroupRule
            properties:
              forProvider:
                properties:
                  cidrIp:
                    description: 'The target IPv4 CIDR address range. Other supported
                      formats include 10.159.6.18/12. Only IPv4 is supported. NOTE:
                      This field has no default value; leaving it empty does not open
                      traffic to all sources but instead fails with MissingParameter.Source
                      (ingress) or MissingParameter.Dest (egress). To allow all IPv4
                      traffic, set cidr_ip = "0.0.0.0/0" explicitly.'
                    type: string
                  description:
                    description: The description of the security group rule. The description
                      can be up to 1 to 512 characters in length. Defaults to null.
                    type: string
                  ipProtocol:
                    description: 'The transport layer protocol of the Security Group
                      Rule. Valid values: tcp, udp, icmp, icmpv6, gre, all. NOTE:
                      From version 1.262.0, ip_protocol can be set to icmpv6.'
                    type: string
                  ipv6CidrIp:
                    description: 'Source IPv6 CIDR address block that requires access.
                      Supports IP address ranges in CIDR format and IPv6 format. NOTE:
                      This parameter cannot be set at the same time as the cidr_ip
                      parameter.'
                    type: string
                  nicType:
                    description: Network type, can be either internet or intranet,
                      the default value is internet.
                    type: string
                  policy:
                    description: 'The action of the Security Group Rule that determines
                      whether to allow inbound access. Default value: accept. Valid
                      values: accept, drop.'
                    type: string
                  portRange:
                    description: |-
                      The range of port numbers relevant to the IP protocol. Default to "-1/-1". When the protocol is tcp or udp, each side port number range from 1 to 65535 and '-1/-1' will be invalid.
                      For example, 1/200 means that the range of the port numbers is 1-200. Other protocols' 'port_range' can only be "-1/-1", and other values will be invalid.
                    type: string
                  prefixListId:
                    description: 'The ID of the source/destination prefix list to
                      which you want to control access. NOTE: If you specify cidr_ip,source_security_group_id,ipv6_cidr_ip
                      parameter, this parameter is ignored.'
                    type: string
                  priority:
                    description: 'The priority of the Security Group Rule. Default
                      value: 1. Valid values: 1 to 100.'
                    type: number
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  securityGroupId:
                    description: The ID of the Security Group.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  sourceGroupOwnerAccount:
                    description: The Alibaba Cloud account of the target security
                      group owner when security groups are authorized across accounts.
                      This field may not take effect. Use source_group_owner_id instead.
                      This parameter is invalid if cidr_ip has already been set.
                    type: string
                  sourceGroupOwnerId:
                    description: The Alibaba Cloud account ID of the target security
                      group owner when security groups are authorized across accounts.
                    type: string
                  sourceSecurityGroupId:
                    description: The target security group ID within the same region.
                      If this field is specified, the nic_type can only select intranet.
                    type: string
                  sourceSecurityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate sourceSecurityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceSecurityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate sourceSecurityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      The type of the Security Group Rule. Valid values:
                      Type of rule, ingress (inbound) or egress (outbound).
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cidrIp:
                    description: 'The target IPv4 CIDR address range. Other supported
                      formats include 10.159.6.18/12. Only IPv4 is supported. NOTE:
                      This field has no default value; leaving it empty does not open
                      traffic to all sources but instead fails with MissingParameter.Source
                      (ingress) or MissingParameter.Dest (egress). To allow all IPv4
                      traffic, set cidr_ip = "0.0.0.0/0" explicitly.'
                    type: string
                  description:
                    description: The description of the security group rule. The description
                      can be up to 1 to 512 characters in length. Defaults to null.
                    type: string
                  ipProtocol:
                    description: 'The transport layer protocol of the Security Group
                      Rule. Valid values: tcp, udp, icmp, icmpv6, gre, all. NOTE:
                      From version 1.262.0, ip_protocol can be set to icmpv6.'
                    type: string
                  ipv6CidrIp:
                    description: 'Source IPv6 CIDR address block that requires access.
                      Supports IP address ranges in CIDR format and IPv6 format. NOTE:
                      This parameter cannot be set at the same time as the cidr_ip
                      parameter.'
                    type: string
                  nicType:
                    description: Network type, can be either internet or intranet,
                      the default value is internet.
                    type: string
                  policy:
                    description: 'The action of the Security Group Rule that determines
                      whether to allow inbound access. Default value: accept. Valid
                      values: accept, drop.'
                    type: string
                  portRange:
                    description: |-
                      The range of port numbers relevant to the IP protocol. Default to "-1/-1". When the protocol is tcp or udp, each side port number range from 1 to 65535 and '-1/-1' will be invalid.
                      For example, 1/200 means that the range of the port numbers is 1-200. Other protocols' 'port_range' can only be "-1/-1", and other values will be invalid.
                    type: string
                  prefixListId:
                    description: 'The ID of the source/destination prefix list to
                      which you want to control access. NOTE: If you specify cidr_ip,source_security_group_id,ipv6_cidr_ip
                      parameter, this parameter is ignored.'
                    type: string
                  priority:
                    description: 'The priority of the Security Group Rule. Default
                      value: 1. Valid values: 1 to 100.'
                    type: number
                  securityGroupId:
                    description: The ID of the Security Group.
                    type: string
                  securityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  securityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate securityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  sourceGroupOwnerAccount:
                    description: The Alibaba Cloud account of the target security
                      group owner when security groups are authorized across accounts.
                      This field may not take effect. Use source_group_owner_id instead.
                      This parameter is invalid if cidr_ip has already been set.
                    type: string
                  sourceGroupOwnerId:
                    description: The Alibaba Cloud account ID of the target security
                      group owner when security groups are authorized across accounts.
                    type: string
                  sourceSecurityGroupId:
                    description: The target security group ID within the same region.
                      If this field is specified, the nic_type can only select intranet.
                    type: string
                  sourceSecurityGroupIdRef:
                    description: Reference to a SecurityGroup in ecs to populate sourceSecurityGroupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  sourceSecurityGroupIdSelector:
                    description: Selector for a SecurityGroup in ecs to populate sourceSecurityGroupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  type:
                    description: |-
                      The type of the Security Group Rule. Valid values:
                      Type of rule, ingress (inbound) or egress (outbound).
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.ipProtocol is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ipProtocol)
                || (has(self.initProvider) && has(self.initProvider.ipProtocol))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: SecurityGroupRuleStatus defines the observed state of SecurityGroupRule.
            properties:
              atProvider:
                properties:
                  cidrIp:
                    description: 'The target IPv4 CIDR address range. Other supported
                      formats include 10.159.6.18/12. Only IPv4 is supported. NOTE:
                      This field has no default value; leaving it empty does not open
                      traffic to all sources but instead fails with MissingParameter.Source
                      (ingress) or MissingParameter.Dest (egress). To allow all IPv4
                      traffic, set cidr_ip = "0.0.0.0/0" explicitly.'
                    type: string
                  description:
                    description: The description of the security group rule. The description
                      can be up to 1 to 512 characters in length. Defaults to null.
                    type: string
                  id:
                    type: string
                  ipProtocol:
                    description: 'The transport layer protocol of the Security Group
                      Rule. Valid values: tcp, udp, icmp, icmpv6, gre, all. NOTE:
                      From version 1.262.0, ip_protocol can be set to icmpv6.'
                    type: string
                  ipv6CidrIp:
                    description: 'Source IPv6 CIDR address block that requires access.
                      Supports IP address ranges in CIDR format and IPv6 format. NOTE:
                      This parameter cannot be set at the same time as the cidr_ip
                      parameter.'
                    type: string
                  nicType:
                    description: Network type, can be either internet or intranet,
                      the default value is internet.
                    type: string
                  policy:
                    description: 'The action of the Security Group Rule that determines
                      whether to allow inbound access. Default value: accept. Valid
                      values: accept, drop.'
                    type: string
                  portRange:
                    description: |-
                      The range of port numbers relevant to the IP protocol. Default to "-1/-1". When the protocol is tcp or udp, each side port number range from 1 to 65535 and '-1/-1' will be invalid.
                      For example, 1/200 means that the range of the port numbers is 1-200. Other protocols' 'port_range' can only be "-1/-1", and other values will be invalid.
                    type: string
                  prefixListId:
                    description: 'The ID of the source/destination prefix list to
                      which you want to control access. NOTE: If you specify cidr_ip,source_security_group_id,ipv6_cidr_ip
                      parameter, this parameter is ignored.'
                    type: string
                  priority:
                    description: 'The priority of the Security Group Rule. Default
                      value: 1. Valid values: 1 to 100.'
                    type: number
                  securityGroupId:
                    description: The ID of the Security Group.
                    type: string
                  securityGroupRuleId:
                    description: The ID of the Security Group Rule.
                    type: string
                  sourceGroupOwnerAccount:
                    description: The Alibaba Cloud account of the target security
                      group owner when security groups are authorized across accounts.
                      This field may not take effect. Use source_group_owner_id instead.
                      This parameter is invalid if cidr_ip has already been set.
                    type: string
                  sourceGroupOwnerId:
                    description: The Alibaba Cloud account ID of the target security
                      group owner when security groups are authorized across accounts.
                    type: string
                  sourceSecurityGroupId:
                    description: The target security group ID within the same region.
                      If this field is specified, the nic_type can only select intranet.
                    type: string
                  type:
                    description: |-
                      The type of the Security Group Rule. Valid values:
                      Type of rule, ingress (inbound) or egress (outbound).
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
