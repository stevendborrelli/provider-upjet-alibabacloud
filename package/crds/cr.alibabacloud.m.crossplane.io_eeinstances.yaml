---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: eeinstances.cr.alibabacloud.m.crossplane.io
spec:
  group: cr.alibabacloud.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: EeInstance
    listKind: EeInstanceList
    plural: eeinstances
    singular: eeinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EeInstance is the Schema for the EeInstances API. Provides a
          Alicloud CR Instance resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EeInstanceSpec defines the desired state of EeInstance
            properties:
              forProvider:
                properties:
                  customOssBucket:
                    description: Custom OSS Bucket name
                    type: string
                  defaultOssBucket:
                    description: 'Whether to use the default OSS Bucket. Value:'
                    type: string
                  imageScanner:
                    description: 'The security scan engine used by the Enterprise
                      Edition of Container Image Service. Value:'
                    type: string
                  instanceName:
                    description: InstanceName
                    type: string
                  instanceType:
                    description: 'The Value configuration of the Group 1 attribute
                      of Container Mirror Service Enterprise Edition. Valid values:'
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to an instance. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating instance with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  namespaceQuota:
                    description: The number of additional namespaces to purchase.
                      The value is an integral multiple of 5.
                    type: number
                  passwordSecretRef:
                    description: Login password, 8-32 digits, must contain at least
                      two letters, symbols, or numbers
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  paymentType:
                    description: 'Payment type, value:'
                    type: string
                  period:
                    description: Prepaid cycle. The unit is Monthly, please enter
                      an integer multiple of 12 for annual paid products.
                    type: number
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  renewPeriod:
                    description: Automatic renewal cycle, in months.
                    type: number
                  renewalStatus:
                    description: 'Automatic renewal status, value:'
                    type: string
                  repoQuota:
                    description: The number of additional repositories to purchase.
                      The value is an integral multiple of 1000.
                    type: number
                  resourceGroupId:
                    description: The ID of the resource group
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcQuota:
                    description: The number of VPC access controls.
                    type: number
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  customOssBucket:
                    description: Custom OSS Bucket name
                    type: string
                  defaultOssBucket:
                    description: 'Whether to use the default OSS Bucket. Value:'
                    type: string
                  imageScanner:
                    description: 'The security scan engine used by the Enterprise
                      Edition of Container Image Service. Value:'
                    type: string
                  instanceName:
                    description: InstanceName
                    type: string
                  instanceType:
                    description: 'The Value configuration of the Group 1 attribute
                      of Container Mirror Service Enterprise Edition. Valid values:'
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to an instance. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating instance with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  namespaceQuota:
                    description: The number of additional namespaces to purchase.
                      The value is an integral multiple of 5.
                    type: number
                  passwordSecretRef:
                    description: Login password, 8-32 digits, must contain at least
                      two letters, symbols, or numbers
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  paymentType:
                    description: 'Payment type, value:'
                    type: string
                  period:
                    description: Prepaid cycle. The unit is Monthly, please enter
                      an integer multiple of 12 for annual paid products.
                    type: number
                  renewPeriod:
                    description: Automatic renewal cycle, in months.
                    type: number
                  renewalStatus:
                    description: 'Automatic renewal status, value:'
                    type: string
                  repoQuota:
                    description: The number of additional repositories to purchase.
                      The value is an integral multiple of 1000.
                    type: number
                  resourceGroupId:
                    description: The ID of the resource group
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcQuota:
                    description: The number of VPC access controls.
                    type: number
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.instanceName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceName)
                || (has(self.initProvider) && has(self.initProvider.instanceName))'
            - message: spec.forProvider.instanceType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.instanceType)
                || (has(self.initProvider) && has(self.initProvider.instanceType))'
            - message: spec.forProvider.paymentType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.paymentType)
                || (has(self.initProvider) && has(self.initProvider.paymentType))'
          status:
            description: EeInstanceStatus defines the observed state of EeInstance.
            properties:
              atProvider:
                properties:
                  createTime:
                    description: The creation time of the resource
                    type: string
                  createdTime:
                    description: (Deprecated since v1.235.0). Field 'created_time'
                      has been deprecated from provider version 1.235.0. New field
                      'create_time' instead.
                    type: string
                  customOssBucket:
                    description: Custom OSS Bucket name
                    type: string
                  defaultOssBucket:
                    description: 'Whether to use the default OSS Bucket. Value:'
                    type: string
                  endTime:
                    description: Expiration Time
                    type: string
                  id:
                    description: The ID of the resource supplied above.
                    type: string
                  imageScanner:
                    description: 'The security scan engine used by the Enterprise
                      Edition of Container Image Service. Value:'
                    type: string
                  instanceEndpoints:
                    description: (Available since v1.240.0) Instance Network Access
                      Endpoint List
                    items:
                      properties:
                        domains:
                          description: Domain List
                          items:
                            properties:
                              domain:
                                description: Domain
                                type: string
                              type:
                                description: Domain Type
                                type: string
                            type: object
                          type: array
                        enable:
                          description: enable
                          type: boolean
                        endpointType:
                          description: Network Access Endpoint Type
                          type: string
                      type: object
                    type: array
                  instanceName:
                    description: InstanceName
                    type: string
                  instanceType:
                    description: 'The Value configuration of the Group 1 attribute
                      of Container Mirror Service Enterprise Edition. Valid values:'
                    type: string
                  kmsEncryptedPassword:
                    description: An KMS encrypts password used to an instance. If
                      the password is filled in, this field will be ignored.
                    type: string
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    description: An KMS encryption context used to decrypt kms_encrypted_password
                      before creating or updating instance with kms_encrypted_password.
                      See Encryption Context. It is valid when kms_encrypted_password
                      is set.
                    type: object
                    x-kubernetes-map-type: granular
                  namespaceQuota:
                    description: The number of additional namespaces to purchase.
                      The value is an integral multiple of 5.
                    type: number
                  paymentType:
                    description: 'Payment type, value:'
                    type: string
                  period:
                    description: Prepaid cycle. The unit is Monthly, please enter
                      an integer multiple of 12 for annual paid products.
                    type: number
                  regionId:
                    description: RegionId
                    type: string
                  renewPeriod:
                    description: Automatic renewal cycle, in months.
                    type: number
                  renewalStatus:
                    description: 'Automatic renewal status, value:'
                    type: string
                  repoQuota:
                    description: The number of additional repositories to purchase.
                      The value is an integral multiple of 1000.
                    type: number
                  resourceGroupId:
                    description: The ID of the resource group
                    type: string
                  status:
                    description: Instance Status
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcQuota:
                    description: The number of VPC access controls.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
